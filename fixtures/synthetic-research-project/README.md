# Synthetic Research Project Fixture

This fixture is a small DevNexus project for exercising DevNexus-Research
setup and worker briefing behavior. It uses synthetic material only and does
not require network access, Zotero, Obsidian, external index APIs, or local
LaTeX compilation.

The project declares research artifact paths under
`.dev-nexus/research/` and includes a minimal LaTeX manuscript under `paper/`.
It is not a real study, paper, dataset, or benchmark.
